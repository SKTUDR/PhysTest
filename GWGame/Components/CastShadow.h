#pragma once

namespace ECS
{

    // ---- CastShadowComp ---------------------------------------------------------
    // このコンポーネントを持つエンティティはシャドウパスの描画対象になる。
    //
    // 持たないエンティティ（床・チェックポイントなど）は
    // シャドウパスをスキップされるのでドローコールを節約できる。
    //
    // 使い分け:
    //   影を落とす → CastShadowComp を追加
    //   影を落とさない → CastShadowComp を付けない（デフォルト）
    // ----------------------------------------------------------------------------
    struct CastShadowComp
    {
    };

} // namespace ECS